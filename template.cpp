#include <iostream>
#include <string>
#include <vector>
using namespace std;

#define LL long long
#define SIZE(x) (int) x.size()

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    return 0;
}
