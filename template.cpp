#include <algorithm>
#include <iostream>
#include <string>
#include <vector>
using namespace std;

#define LL long long
#define SIZE(x) (int) x.size()
#define PB(x) push_back(x)

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(NULL);

    return 0;
}
